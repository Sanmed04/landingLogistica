---
import JSON from "../../public/JSON.json";
import Card from "./Card.astro";
---

<h2 class="font-bold text-6xl">Por qué usar Logistica</h2>
<div class="flex-wrap w-1/3 mx-auto pt-6 pb-16">
  <p class="leading-loose text-lg">
    Elige (nombre de empresa) de logística para envíos eficientes y confiables.
    Nuestro equipo experto se encarga de todo, permitiéndote concentrarte en lo
    que más importa: tu negocio.
  </p>
</div>
<div class="flex grid-cols-3 justify-evenly">
  {
    JSON.cardArray.map((card, index) => (
      <Card
        key={index}
        title={card.title}
        text={card.text}
        image={card.image}
        alt={card.alt}
      />
    ))
  }
</div>
